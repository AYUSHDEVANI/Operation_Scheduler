const logger = require('../logs/logger');

// Placeholder for future business logic separate from controllers
const exampleService = () => {
  logger.info('Service called');
};

module.exports = { exampleService };
